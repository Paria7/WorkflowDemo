# @bci-generic-web-styles

Bosch corporate stylesheets and mixins. This package also includes bosch fonts (iconfont as well) and images.

## Build

Run `npm run build` to build the project. The build artifacts will be stored in the `dist/` directory.

